#!/bin/sh

# Script para mostrar el uso del disco

$HIDE_NOTIFICATIONS

TOTAL=$(df -h /home | awk '/[0-9]/ {print $2}')
USADO=$(df -h /home | awk '/[0-9]/ {print $3}')
LIBRE=$(df -h /home | awk '/[0-9]/ {print $4}')

notify-send -e "Disco ($TOTAL)" "Usados: $USADO\nLibre: $LIBRE"
